# Security Model

EphemGuard follows defense-in-depth and least privilege. No semantic classifier is treated as the final authorization boundary. Deterministic policy, path confinement, capability leases, schema integrity, and replay controls provide the hard checks.
