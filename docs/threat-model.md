# Threat Model

Assets: local files, credentials, source code, command execution capability, tool configuration.

Threats: indirect prompt injection, malicious tool descriptions, path traversal, command injection, replay of stale capabilities, accidental over-privilege.

Out of scope for the starter: kernel compromise, malicious local administrator, compromised Python runtime, and a fully implemented distributed nonce store.
